<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.argus.rag.ingestion.mapper.IngestionJobMapper">

    <resultMap id="IngestionJobResultMap" type="com.argus.rag.ingestion.model.entity.IngestionJobEntity">
        <id property="id" column="id"/>
        <result property="documentId" column="document_id"/>
        <result property="groupId" column="group_id"/>
        <result property="jobType" column="job_type"/>
        <result property="status" column="status"/>
        <result property="retryCount" column="retry_count"/>
        <result property="maxRetries" column="max_retries"/>
        <result property="workerId" column="worker_id"/>
        <result property="startedAt" column="started_at"/>
        <result property="finishedAt" column="finished_at"/>
        <result property="nextRetryAt" column="next_retry_at"/>
        <result property="lastError" column="last_error"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectRunnableJobs" resultMap="IngestionJobResultMap">
        select id,
               document_id,
               group_id,
               job_type,
               status,
               retry_count,
               max_retries,
               worker_id,
               started_at,
               finished_at,
               next_retry_at,
               last_error,
               created_at,
               updated_at
        from ingestion_jobs jobs
        where status = #{status}
          and (next_retry_at is null or next_retry_at &lt;= #{now})
          and exists (
              select 1
              from documents documents
              where documents.id = jobs.document_id
                and documents.group_id = jobs.group_id
                and documents.deleted = false
          )
        order by created_at asc, id asc
        limit #{limit}
    </select>

    <update id="claimRunning">
        update ingestion_jobs as jobs
        set status = #{runningStatus},
            worker_id = #{workerId},
            started_at = #{startedAt},
            finished_at = null,
            last_error = null,
            updated_at = now()
        where id = #{jobId}
          and status = #{pendingStatus}
          and (next_retry_at is null or next_retry_at &lt;= #{startedAt})
          and exists (
              select 1
              from documents documents
              where documents.id = jobs.document_id
                and documents.group_id = jobs.group_id
                and documents.deleted = false
          )
    </update>

    <update id="markSucceeded">
        update ingestion_jobs
        set status = #{succeededStatus},
            finished_at = #{finishedAt},
            next_retry_at = null,
            last_error = null,
            updated_at = now()
        where id = #{jobId}
          and status = #{runningStatus}
          and worker_id = #{workerId}
    </update>

    <update id="markFailed">
        update ingestion_jobs
        set status = #{failedStatus},
            retry_count = retry_count + 1,
            finished_at = #{finishedAt},
            next_retry_at = null,
            last_error = #{lastError},
            updated_at = now()
        where id = #{jobId}
          and status = #{runningStatus}
          and worker_id = #{workerId}
    </update>
</mapper>
