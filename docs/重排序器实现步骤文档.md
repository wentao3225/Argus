# LLM Reranker 实现步骤文档

> 按照本文档的步骤逐一操作，即可在 Argus 中完成重排序器的集成。

---

## 零、设计概览

### 0.1 重排器在检索链路中的位置

```
用户问题
  → Query Planning（查询规划）
  → 向量检索（top-50）+ 关键词检索（top-50）
  → RRF 融合排序（所有候选按 RRF 分数排列）
  → 【重排序器在这里插入】取 top-20 候选，LLM 精细打分
  → 按 LLM 相关性分数降序，取 top-5
  → 聚类分组 + 邻居窗口扩展
  → 证据评估
  → 返回结果
```

### 0.2 为什么 RRF 之后加重排

- RRF 只在排名层面做融合，不知道 "query 和 chunk 内容到底多相关"
- 重排序器用 LLM 读 query + chunk 文本，理解细粒度语义匹配
- 可以把"排名高但不相关的噪声"压下去，把"排名中等但高度相关"的推上来

### 0.3 重排策略

- **取 RRF 融合后的 top-20 候选**（不是 50，因为 LLM 调用有延迟和成本）
- **每批次 5 个候选**，4 个并行调用 LLM → 总延迟 ≈ 1 次 LLM 调用时间
- **配置化开关**：dev 环境默认开启，可通过配置关闭回退到纯 RRF
- **安全回退**：重排失败时直接返回 RRF 原始排序结果，不中断链路

### 0.4 需要创建/修改的文件清单

| 操作 | 文件 | 说明 |
|------|------|------|
| **新建** | `.../rag/ChunkRerankerService.java` | 核心重排服务 |
| **新建** | `.../rag/RerankerResult.java` | 重排结果 record |
| **新建** | `.../config/RerankerConfiguration.java` | ChatClient + Prompt |
| **新建** | `resources/prompts/reranker/user.st` | LLM 打分 Prompt 模板 |
| **修改** | `.../HybridChunkRetrievalService.java` | 集成重排器 |
| **修改** | `resources/application-dev.yml` | 配置开关 |
| **修改** | `.../RagRetrievalEvaluationTest.java` | 增加 4 列指标 |

---

## 一、创建重排器 Prompt 模板

### 步骤 1.1

创建文件：`src/main/resources/prompts/reranker/user.st`

```st
你是一名文档相关性评估专家。请评估每条候选文本与用户问题的相关性，输出一个 JSON 对象。

用户问题：
{question}

候选文本列表：
{candidates}

输出格式（严格 JSON）：
{
  "scores": [
    {"candidateId": "文本的唯一标识", "score": 0.85, "reason": "简短理由（≤10字）"},
    ...
  ]
}

评分规则：
1. score 范围 0.0 ~ 1.0，1.0 表示完全匹配，0.0 表示完全不相关。
2. 评分标准：
   - 1.0：候选内容直接、完整地回答了问题
   - 0.8 ~ 0.99：候选内容与问题高度相关，包含关键信息但不够完整
   - 0.5 ~ 0.79：候选内容部分相关，涉及问题中的部分概念
   - 0.2 ~ 0.49：候选内容弱相关，仅提到个别相关词汇
   - 0.0 ~ 0.19：候选内容基本不相关
3. reason 要简短明确，说明相关或不相关的原因，不超过 10 字。
4. 输出纯 JSON，不要包含 Markdown 标记、代码块、解释说明。
```

---

## 二、创建 RerankerResult 记录类

### 步骤 2.1

创建文件：`src/main/java/com/argus/rag/qa/rag/RerankerResult.java`

```java
package com.argus.rag.qa.rag;

/**
 * 重排序器的单条评分结果。
 *
 * @param candidateId 候选切片标识（使用 RRF 阶段分配的临时编号）
 * @param score       LLM 评定的相关性分数 [0.0, 1.0]
 * @param reason      相关性简短理由（≤10 字）
 */
public record RerankerResult(
        String candidateId,
        double score,
        String reason
) {
}
```

---

## 三、创建 ChunkRerankerService

### 步骤 3.1

创建文件：`src/main/java/com/argus/rag/qa/rag/ChunkRerankerService.java`

```java
package com.argus.rag.qa.rag;

import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.ObjectMapper;
import lombok.extern.slf4j.Slf4j;
import org.springframework.ai.chat.client.ChatClient;
import org.springframework.ai.chat.prompt.PromptTemplate;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.stereotype.Service;
import org.springframework.util.StringUtils;

import java.util.*;
import java.util.stream.Collectors;

/**
 * LLM 驱动的切片重排序服务。
 * <p>
 * 在 RRF 粗排之后，取 top-20 候选切片，用 LLM 评估
 * query 与每个候选切片内容的细粒度相关性，按相关性分数重新排序。
 * </p>
 *
 * <h3>性能策略</h3>
 * <ul>
 * <li>只重排 top-20 候选（不是全部 50+），减少 LLM 调用成本</li>
 * <li>每批 5 个候选，4 批内完成</li>
 * <li>重排失败时直接返回原始排序结果，不中断检索链路</li>
 * </ul>
 */
@Slf4j
@Service
@ConditionalOnProperty(value = "rag.qa.reranker.enabled", havingValue = "true", matchIfMissing = false)
public class ChunkRerankerService {

    /**
     * 进入重排器的最大候选数。
     * RRF 融合后可能有 50+ 候选，只取前 20 个给 LLM 打分。
     */
    private static final int RERANK_CANDIDATE_K = 20;

    /**
     * 每批发送给 LLM 的候选数。
     * 每批 5 个候选 + 1 个 query，控制 Prompt 总长度。
     */
    private static final int BATCH_SIZE = 5;

    private final ChatClient rerankerChatClient;
    private final PromptTemplate rerankerUserPromptTemplate;
    private final ObjectMapper objectMapper;

    /**
     * 构造函数。
     *
     * @param rerankerChatClient         重排序专用的 ChatClient
     * @param rerankerUserPromptTemplate 重排序 Prompt 模板
     * @param objectMapper               JSON 解析
     */
    public ChunkRerankerService(
            @Qualifier("rerankerChatClient") ChatClient rerankerChatClient,
            @Qualifier("rerankerUserPromptTemplate") PromptTemplate rerankerUserPromptTemplate,
            ObjectMapper objectMapper) {
        this.rerankerChatClient = rerankerChatClient;
        this.rerankerUserPromptTemplate = rerankerUserPromptTemplate;
        this.objectMapper = objectMapper;
    }

    /**
     * 对 RRF 粗排后的候选切片进行 LLM 重排序。
     * <p>
     * 流程：取前 20 候选 → 拼装 batch → 逐批调用 LLM 打分 → 汇总排序。
     * 整个过程不修改原有候选对象，只返回重新排序后的列表。
     * </p>
     *
     * @param question           用户原始问题
     * @param rankedCandidates   RRF 粗排后的全部候选（已按 RRF 分数降序排列）
     * @return 重排后的候选列表（仍为原有候选对象，仅顺序改变）
     */
    public List<HybridChunkRetrievalService.RetrievalCandidate> rerank(
            String question,
            List<HybridChunkRetrievalService.RetrievalCandidate> rankedCandidates) {

        if (rankedCandidates == null || rankedCandidates.size() <= 1) {
            return rankedCandidates;
        }

        // 只取 top-RERANK_CANDIDATE_K 给 LLM 打分
        List<HybridChunkRetrievalService.RetrievalCandidate> toRerank =
                rankedCandidates.stream()
                        .limit(RERANK_CANDIDATE_K)
                        .toList();

        log.info("重排序开始: candidateCount={}, toRerank={}, batchSize={}",
                rankedCandidates.size(), toRerank.size(), BATCH_SIZE);

        // 逐批次调用 LLM，汇总所有得分
        Map<Long, RerankerResult> scoreById = new LinkedHashMap<>();
        long startNano = System.nanoTime();

        for (int batchStart = 0; batchStart < toRerank.size(); batchStart += BATCH_SIZE) {
            int batchEnd = Math.min(batchStart + BATCH_SIZE, toRerank.size());
            List<HybridChunkRetrievalService.RetrievalCandidate> batch =
                    toRerank.subList(batchStart, batchEnd);
            try {
                List<RerankerResult> batchResults = scoreBatch(question, batch);
                for (int i = 0; i < batch.size(); i++) {
                    scoreById.put(batch.get(i).chunkId(), batchResults.get(i));
                }
            } catch (Exception e) {
                log.warn("重排序批次失败 (chunks {}-{}), 跳过此批次: {}",
                        batchStart, batchEnd - 1, e.getMessage());
                // 失败的批次给默认分 0.5，不中断
                for (int i = 0; i < batch.size(); i++) {
                    scoreById.put(batch.get(i).chunkId(),
                            new RerankerResult("c" + batch.get(i).chunkId(), 0.5, "评分失败"));
                }
            }
        }

        long elapsedMs = (System.nanoTime() - startNano) / 1_000_000;

        // 按 LLM 分数降序排列（分数相同时保持 RRF 原始顺序）
        List<HybridChunkRetrievalService.RetrievalCandidate> reranked = new ArrayList<>(rankedCandidates);
        reranked.sort(Comparator
                .<HybridChunkRetrievalService.RetrievalCandidate, Double>comparing(
                        c -> scoreById.containsKey(c.chunkId())
                                ? scoreById.get(c.chunkId()).score()
                                : c.rankingScore())  // 不在 top-20 的用 RRF 分
                .reversed()
                .thenComparingDouble(HybridChunkRetrievalService.RetrievalCandidate::rankingScore)
                .reversed());

        log.info("重排序完成: totalCandidates={}, reranked={}, scored={}, elapsedMs={}",
                rankedCandidates.size(), reranked.size(), scoreById.size(), elapsedMs);

        return reranked;
    }

    /**
     * 对一个批次的候选切片调用 LLM 打分。
     * <p>
     * 构造 Prompt → 发送 → 解析 JSON → 返回分数列表。
     * 若 LLM 返回格式错误，给每个候选默认 0.5 分。
     * </p>
     *
     * @param question 用户问题
     * @param batch    本批次候选切片（1~5 个）
     * @return 本批次各候选的评分
     */
    private List<RerankerResult> scoreBatch(
            String question,
            List<HybridChunkRetrievalService.RetrievalCandidate> batch) {

        // 构造 "c{chunkId}: {前200字}" 格式的候选文本
        String candidatesText = batch.stream()
                .map(c -> String.format("c%d: %s",
                        c.chunkId(),
                        truncateText(c.chunkText(), 200)))
                .collect(Collectors.joining("\n\n"));

        // 填充 Prompt 模板
        String promptContent = rerankerUserPromptTemplate.create(
                Map.of("question", question, "candidates", candidatesText)
        ).getContents();

        // 调用 LLM
        String response = rerankerChatClient.prompt()
                .user(promptContent)
                .call()
                .content();

        // 解析 JSON 响应
        return parseResponse(response, batch);
    }

    /**
     * 解析 LLM 返回的 JSON，提取 scores 数组。
     * <p>
     * 支持两种格式：LLM 直接返回 JSON 对象，或返回含 Markdown 代码块包裹的 JSON。
     * 找不到合法 scores 数组时，给每个候选默认 0.5 分。
     * </p>
     */
    private List<RerankerResult> parseResponse(
            String rawResponse,
            List<HybridChunkRetrievalService.RetrievalCandidate> batch) {

        if (!StringUtils.hasText(rawResponse)) {
            log.warn("重排序 LLM 返回空内容，使用默认分");
            return batch.stream()
                    .map(c -> new RerankerResult("c" + c.chunkId(), 0.5, "LLM返回空"))
                    .toList();
        }

        // 如果 LLM 返回了 Markdown 代码块，提取 JSON 部分
        String json = rawResponse.trim();
        if (json.startsWith("```")) {
            json = json.replaceAll("```json\\s*", "")
                    .replaceAll("```\\s*", "")
                    .trim();
        }

        try {
            Map<String, List<RerankerResult>> parsed = objectMapper.readValue(
                    json, new TypeReference<>() {});
            List<RerankerResult> scores = parsed.get("scores");
            if (scores == null || scores.isEmpty()) {
                throw new IllegalArgumentException("scores 为空");
            }
            return scores;
        } catch (Exception e) {
            log.warn("重排序 JSON 解析失败: {}. rawResponse前100字: {}",
                    e.getMessage(),
                    rawResponse.length() > 100 ? rawResponse.substring(0, 100) : rawResponse);
            // 解析失败给默认分
            return batch.stream()
                    .map(c -> new RerankerResult("c" + c.chunkId(), 0.5, "JSON解析失败"))
                    .toList();
        }
    }

    /**
     * 截断文本到指定最大长度。
     */
    private String truncateText(String text, int maxLength) {
        if (text == null) return "";
        return text.length() <= maxLength ? text : text.substring(0, maxLength);
    }
}
```

### 步骤 3.2 注意：RetrievalCandidate 的 chunkText 字段

上面用到了 `c.chunkText()`。你需要确认 `RetrievalCandidate` 内部类有 `chunkText` 字段和 getter。检查当前代码——如果 `RetrievalCandidate` 没有存储 `chunkText`，需要在以下位置补充：

**3.2.1** 给 `RetrievalCandidate` 添加 `chunkText` 字段：

在 `HybridChunkRetrievalService.java` 的 `RetrievalCandidate` 内部类中，找到字段声明区域（约 line 519），添加：

```java
/**
 * 切片文本内容（供重排序器使用）
 */
private final String chunkText;
```

**3.2.2** 修改构造函数，增加 `chunkText` 参数：

```java
private RetrievalCandidate(Long documentId, Long chunkId, Integer chunkIndex, String chunkText) {
    this.documentId = documentId;
    this.chunkId = chunkId;
    this.chunkIndex = chunkIndex;
    this.chunkText = chunkText;
}
```

**3.2.3** 修改 `fromVectorHit` 和 `fromKeywordHit` 工厂方法：

```java
static RetrievalCandidate fromVectorHit(PgVectorRetrievalAdapter.VectorHit hit) {
    return new RetrievalCandidate(hit.documentId(), hit.chunkId(), hit.chunkIndex(), hit.chunkText());
}

static RetrievalCandidate fromKeywordHit(PgKeywordSearchService.KeywordHit hit) {
    return new RetrievalCandidate(hit.documentId(), hit.chunkId(), hit.chunkIndex(), hit.chunkText());
}
```

**3.2.4** 添加 `chunkText()` getter（与其他 getter 一起）：

```java
String chunkText() {
    return chunkText;
}
```

**3.2.5** 确认 `VectorHit` 和 `KeywordHit` 有 `chunkText()` 方法。用 IDE 搜索这些类的定义确认。如果它们没有 `chunkText()`，可以退一步：不存储 chunkText 到 RetrievalCandidate，而是在 ChunkRerankerService 中通过 `selectQaReadyChunksByIds` 批量查询。但这样会增加一次 DB 查询。推荐在 VectorHit / KeywordHit 里确认有 chunkText——通常向量检索和关键词检索都会返回 chunk 文本。

---

## 四、创建配置类

### 步骤 4.1

创建文件：`src/main/java/com/argus/rag/qa/config/RerankerConfiguration.java`

```java
package com.argus.rag.qa.config;

import org.springframework.ai.chat.client.ChatClient;
import org.springframework.ai.chat.prompt.PromptTemplate;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.core.io.ClassPathResource;

/**
 * 重排序器配置类。
 * <p>
 * 通过 {@code rag.qa.reranker.enabled=true} 启用。
 * 仅在启用时才创建 ChatClient 和 PromptTemplate Bean，
 * 此时 {@link com.argus.rag.qa.rag.ChunkRerankerService} 也会被激活。
 * </p>
 */
@Configuration
@ConditionalOnProperty(value = "rag.qa.reranker.enabled", havingValue = "true")
public class RerankerConfiguration {

    /**
     * 创建重排序专用的 ChatClient。
     * <p>与 QA 用同一个模型（Kimimoonshot-v1-8k），但独立配置便于后续更换。</p>
     *
     * @param chatClientBuilder Spring AI 提供的 ChatClient 构造器
     * @return 重排序专用的 ChatClient 实例
     */
    @Bean("rerankerChatClient")
    public ChatClient rerankerChatClient(ChatClient.Builder chatClientBuilder) {
        return chatClientBuilder.build();
    }

    /**
     * 加载重排序 Prompt 模板。
     * <p>模板文件路径：{@code prompts/reranker/user.st}</p>
     *
     * @return 重排序 Prompt 模板
     */
    @Bean("rerankerUserPromptTemplate")
    public PromptTemplate rerankerUserPromptTemplate() {
        return PromptTemplate.builder()
                .resource(new ClassPathResource("prompts/reranker/user.st"))
                .build();
    }
}
```

---

## 五、添加配置开关

### 步骤 5.1

修改 `src/main/resources/application-dev.yml`，在 `rag.qa:` 配置块下添加：

```yaml
rag:
  auth:
    jwt-secret: ${rag.auth.jwt-secret}
    # ... 保留原有配置 ...
  qa:
    query-planning-enabled: true
    # ── 重排序器配置 ──
    reranker:
      # 是否启用 LLM 重排序（dev 默认开启，prod 可按需关闭节约成本）
      enabled: true
```

---

## 六、集成到 HybridChunkRetrievalService

### 步骤 6.1 注入重排器

在 `HybridChunkRetrievalService.java` 头部添加注入（与其它 `private final` 字段放在一起）：

```java
/**
 * LLM 重排序服务（可选，通过配置开关控制）
 */
private final ChunkRerankerService chunkRerankerService;
```

### 步骤 6.2 修改构造函数

在主构造函数中添加参数（Spring 会自动处理 `@Autowired(required = false)` → 当 reranker 未启用时为 null）：

```java
@Autowired
public HybridChunkRetrievalService(
        PgVectorRetrievalAdapter vectorRetrievalAdapter,
        PgKeywordSearchService pgKeywordSearchService,
        DocumentChunkMapper documentChunkMapper,
        QueryPlanningService queryPlanningService,
        ChunkRerankerService chunkRerankerService) {    // ← 新增
    this(
            vectorRetrievalAdapter,
            pgKeywordSearchService,
            documentChunkMapper,
            queryPlanningService,
            DEFAULT_NEIGHBOR_WINDOW,
            chunkRerankerService);                       // ← 新增
}

public HybridChunkRetrievalService(
        PgVectorRetrievalAdapter vectorRetrievalAdapter,
        PgKeywordSearchService pgKeywordSearchService,
        DocumentChunkMapper documentChunkMapper,
        QueryPlanningService queryPlanningService,
        int neighborWindow,
        ChunkRerankerService chunkRerankerService) {     // ← 新增
    this.vectorRetrievalAdapter = vectorRetrievalAdapter;
    this.pgKeywordSearchService = pgKeywordSearchService;
    this.documentChunkMapper = documentChunkMapper;
    this.queryPlanningService = queryPlanningService;
    this.neighborWindow = Math.max(0, neighborWindow);
    this.chunkRerankerService = chunkRerankerService;    // ← 新增
}
```

### 步骤 6.3 在 RRF 融合之后插入重排调用

在 `retrieve()` 方法中，找到 RRF 融合排序后的代码段（约在 `rankedCandidates` 排序后、`buildClusters` 之前），插入重排调用：

```java
// 排序（不在排序阶段截断，避免聚类吞掉候选导致输出不足 topK）
List<RetrievalCandidate> rankedCandidates = candidates.values().stream()
        .sorted(Comparator
                .comparingDouble(RetrievalCandidate::rankingScore).reversed()
                .thenComparing(RetrievalCandidate::chunkId))
        .toList();

// ═══ 重排序器插在这里 ═══
// 用 LLM 对 RRF 粗排后的候选做精细相关性评估
if (chunkRerankerService != null) {
    rankedCandidates = chunkRerankerService.rerank(normalizedQuestion, rankedCandidates);
}
// ═══════════════════════════

// 聚类分组：仅用于邻居窗口扩展，不决定输出数量
List<RetrievalCluster> rankedClusters = buildClusters(rankedCandidates);
```

**关键**：重排 `rankedCandidates` 发生在聚类之前，所以聚类和后续的 topK 截断都使用重排后的顺序。

---

## 七、更新评估测试

### 步骤 7.1 混合检索评估中添加重排对比

修改 `RagRetrievalEvaluationTest.java`，在混合检索结果之后、汇总之前，增加"混合检索+重排"的第四列：

**7.1.1** 在测试类中添加一个重排专用的检索方法调用。由于评估测试直接调 `hybridChunkRetrievalService.retrieve()`，而重排已在 `retrieve()` 内部集成，因此**无需修改测试代码**——只要 reranker 配置开启，`retrieve()` 返回的结果就已经经过重排了。

**7.1.2** 如果想对比"无重排 vs 有重排"的效果，可以临时关闭配置跑一次，开启跑一次，对比两份报告。更优雅的做法是在测试中动态注入——但为了简洁，推荐先跑有重排的，只出一份报告。

**7.1.3** 更新报告中的方法名称（可选）：

在 `generateMarkdownReport()` 中，把 "混合检索" 改为 "混合检索+重排" 或 "混合检索（含 LLM 重排）"，让报告读者知道重排已生效。

```java
// 在报告生成函数中，将表头和方法名称更新：
.append("| 混合检索+重排 | **").append(fmt(summary.avgHybridRecall())).append("** |")
```

---

## 八、编译验证

### 步骤 8.1

```bash
cd Argus-backend
mvn test-compile
```

预期结果：编译成功，无错误。

可能的编译问题及解决：

| 问题 | 原因 | 解决 |
|------|------|------|
| `cannot find symbol: chunkText` | `VectorHit`/`KeywordHit` 没有 `chunkText` 字段 | 用 IDE 跳转查看这些类的 getter，调整 3.2.3 的代码 |
| `ChunkRerankerService` Bean 注入失败 | 配置未开启 | 确认 `application-dev.yml` 有 `rag.qa.reranker.enabled: true` |
| `@ConditionalOnProperty` 不生效 | 没有 `spring-boot-autoconfigure` 依赖 | 添加 `@EnableConfigurationProperties` 或直接用 `@Profile("dev")` |

---

## 九、运行评估

### 步骤 9.1

```bash
mvn test "-Dtest=RagRetrievalEvaluationTest" "-DfailIfNoTests=false" "-Dspring.profiles.active=dev"
```

### 步骤 9.2 预期效果

- **Precision@5 提升**（最明显）：噪声候选被 LLM 压分后退出 top5
- **Recall@5 改善**（间接）：正确候选被 LLM 从 rank 6~20 推入 top5
- **延迟略微增加**：每次检索额外 ~1-2 秒（4 批次 × LLM 调用）

### 步骤 9.3 面试口径准备

**Q：为什么加 LLM 重排序器？**
> RRF 只做排名位置融合，不理解 query 和 chunk 的语义匹配。re-ranker 用 LLM 读 query+chunk 做精细打分，把"排名高但不相关"的噪声压下去。

**Q：为什么用 LLM 而不是 cross-encoder？**
> LLM 不需要引入 Python 依赖和额外模型部署，和现有 Java 技术栈一致。只对 top-10~20 做重排，延迟控制在 1~2 秒。未来数据规模上来了可以迁移到 cross-encoder（bge-reranker-v2-m3），接口已预留。

**Q：怎么控制成本？**
> 只重排 top-20 候选 + 每批 5 个 = 最多 4 次 LLM 调用。比每次 QA 的主 LLM 调用开销小。还有配置开关可以根据环境随时关闭。

**Q：有量化数据吗？**
> 评估报告里有对比数据（混合检索 vs 混合检索+重排），Precision@5 提升 xx%，Recall@5 提升 xx%，MRR 提升 xx%。报告可以现场演示。

---

## 十、附录：文件结构总览

```
Argus-backend/
├── src/main/java/com/argus/rag/qa/
│   ├── config/
│   │   ├── QaChatClientConfiguration.java      (保持不变)
│   │   ├── QueryPlanningConfiguration.java     (保持不变)
│   │   └── RerankerConfiguration.java          (新建 ← 步骤四)
│   ├── rag/
│   │   ├── ChunkRerankerService.java            (新建 ← 步骤三)
│   │   ├── HybridChunkRetrievalService.java     (修改 ← 步骤六)
│   │   └── RerankerResult.java                  (新建 ← 步骤二)
│   └── ...
├── src/main/resources/
│   ├── application-dev.yml                      (修改 ← 步骤五)
│   └── prompts/
│       └── reranker/
│           └── user.st                           (新建 ← 步骤一)
├── src/test/java/com/argus/rag/qa/rag/
│   └── RagRetrievalEvaluationTest.java          (可选修改 ← 步骤七)
└── docs/
    └── 重排序器实现步骤文档.md                    (本文档)
```
